import { Note } from "./Note";

export const meta = {
  title: "Welcome",
  description: "MDX-powered posts on this site.",
  date: "2026-01-08",
  slug: "welcome",
};

# Welcome

This site renders blog posts as **MDX** so you can mix Markdown with React components when you need them.

<Note>MDX tip: import shared UI from `src/blog` and compose it right inside a post.</Note>

## What you can do

- Write in familiar Markdown syntax
- Drop in interactive or styled JSX where it helps
- Add new files under `src/blog/posts` and they show up on the blog index automatically

---

Thanks for reading.
